info:
  name: PatientService- Patients Registered
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/patients
  body:
    type: json
    data: |-
      {
        "name" : "John Doe"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
